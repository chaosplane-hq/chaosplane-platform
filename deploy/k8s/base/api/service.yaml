apiVersion: v1
kind: Service
metadata:
  name: go-api
  namespace: chaosplane-system
spec:
  selector:
    app.kubernetes.io/name: go-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
